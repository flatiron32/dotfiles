#!/usr/bin/env bash
########################################
####     SOURCE
########################################

# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
    . "$HOME/.bashrc"
    fi
fi

export REQUESTS_CA_BUNDLE=$HOME/.zscaler/certs.pem
export SSL_CERT_FILE=$HOME/.zscaler/certs.pem
export NODE_EXTRA_CA_CERTS=$HOME/.zscaler/certs.pem
export CURL_CA_BUNDLE=$HOME/.zscaler/certs.pem
export ca_certificate=$HOME/.zscaler/certs.pem
export AWS_CA_BUNDLE=$HOME/.zscaler/certs.pem

complete -C /opt/homebrew/bin/vault vault

### MANAGED BY RANCHER DESKTOP START (DO NOT EDIT)
export PATH="$HOME/.rd/bin:$PATH"
### MANAGED BY RANCHER DESKTOP END (DO NOT EDIT)

